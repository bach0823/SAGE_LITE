model: B1
seed: 42
img_size: 448
batch_size: 20
num_workers: 4
epochs: 30
stage1_epochs: 15
patience: 6
lr: 1e-4
output_dir: "/content/drive/MyDrive/crack_seg/B1_Crack500_Run1"
mask_suffix: ""
smart_filter: true
crop_mode: "random"

# root_dir — override per environment:
#   Colab : root_dir: "/content/dataset/Crack500"
#   Local (canonical pre-cropped): root_dir: "D:/truong/SpecialSubjectTTNT/datasets/CRACK500_canonical"
root_dir: "/content/dataset/Crack500"

train:
  images: "train/images"
  masks: "train/masks"

val:
  images: "val/images"
  masks: "val/masks"

test:
  images: "test/images"
  masks: "test/masks"
